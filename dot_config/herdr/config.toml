# Herdr configuration
# Managed by chezmoi at ~/.config/herdr/config.toml.
# Terminal-native workspace/tab/pane manager.

onboarding = false

[terminal]
# Use Fish while preserving macOS login shell behavior.
default_shell = "fish"
shell_mode = "auto"
# New panes and tabs start in the current pane's directory.
new_cwd = "follow"

[theme]
name = "catppuccin"

[theme.custom]
# Catppuccin Macchiato-inspired overrides.
panel_bg = "#24273a"
accent = "#c6a0f6"
blue = "#8aadf4"
green = "#a6da95"
red = "#ed8796"
yellow = "#eed49f"

[update]
channel = "stable"

[keys]
# Match tmux: C-Space.
prefix = "ctrl+space"

# Basics.
help = "prefix+?"
settings = "prefix+shift+s"
detach = "prefix+d"
reload_config = "prefix+r"
goto = "prefix+semicolon"
open_notification_target = "prefix+o"

# Workspace controls:
#   prefix+w       choose workspace
#   prefix+shift+w rename workspace (avoids symbol-layer gymnastics)
#   prefix+( / )   previous / next workspace
workspace_picker = "prefix+w"
new_workspace = "prefix+shift+n"
new_worktree = "prefix+shift+t"
rename_workspace = "prefix+shift+w"
close_workspace = "prefix+shift+d"
previous_workspace = "prefix+("
next_workspace = "prefix+)"
switch_workspace = "prefix+shift+1..9"

# agents
focus_agent = "prefix+alt+1..9"


# Tab controls.
new_tab = "prefix+c"
previous_tab = "prefix+p"
next_tab = "prefix+n"
switch_tab = "alt+1..9"
rename_tab = "prefix+comma"
close_tab = "prefix+ampersand"
rename_pane = "prefix+shift+p"
edit_scrollback = "prefix+e"

# Pane creation:
#   prefix+| split right
#   prefix+- split down
split_vertical = "prefix+|"
split_horizontal = "prefix+minus"
close_pane = "prefix+x"
zoom = "prefix+z"
copy_mode = "prefix+["

# Pane resize mode.
resize_mode = "prefix+shift+r"

# Pane navigation; Vim/Neovim-aware navigation owns the direct chords.
# Prefix bindings remain available as an escape hatch from any pane.
focus_pane_left = "prefix+h"
focus_pane_down = "prefix+j"
focus_pane_up = "prefix+k"
focus_pane_right = "prefix+l"
cycle_pane_next = "prefix+tab"
cycle_pane_previous = "prefix+shift+tab"

# Match tmux's status toggle.
toggle_sidebar = "prefix+b"

# Navigate-mode movement.
navigate_workspace_up = "up"
navigate_workspace_down = "down"
navigate_pane_left = "h"
navigate_pane_down = "j"
navigate_pane_up = "k"
navigate_pane_right = "l"

# vim-herdr-navigation forwards these chords into Vim/Neovim when focused;
# otherwise it focuses the adjacent Herdr pane.
[[keys.command]]
key = "ctrl+h"
type = "plugin_action"
command = "vim-herdr-navigation.left"
description = "Navigate left (Vim/Herdr)"

[[keys.command]]
key = "ctrl+j"
type = "plugin_action"
command = "vim-herdr-navigation.down"
description = "Navigate down (Vim/Herdr)"

[[keys.command]]
key = "ctrl+k"
type = "plugin_action"
command = "vim-herdr-navigation.up"
description = "Navigate up (Vim/Herdr)"

[[keys.command]]
key = "ctrl+l"
type = "plugin_action"
command = "vim-herdr-navigation.right"
description = "Navigate right (Vim/Herdr)"

# Match tmux's three-cell prefix+arrow pane resizing.
[[keys.command]]
key = "prefix+left"
type = "shell"
command = "herdr pane resize --current --direction left --amount 3"
description = "Resize pane left"

[[keys.command]]
key = "prefix+down"
type = "shell"
command = "herdr pane resize --current --direction down --amount 3"
description = "Resize pane down"

[[keys.command]]
key = "prefix+up"
type = "shell"
command = "herdr pane resize --current --direction up --amount 3"
description = "Resize pane up"

[[keys.command]]
key = "prefix+right"
type = "shell"
command = "herdr pane resize --current --direction right --amount 3"
description = "Resize pane right"

# Match tmux's terminal-tool popups.
[[keys.command]]
key = "prefix+g"
type = "popup"
command = "lazygit"
description = "Open lazygit"

[[keys.command]]
key = "prefix+shift+g"
type = "popup"
command = "jjui"
description = "Open jjui"

# Fleet/codex counterpart to cmux's Kim workspace launcher.
[[keys.command]]
key = "prefix+shift+k"
type = "shell"
command = "fleet herdr kim"
description = "Open Kim in a new Herdr workspace"

[ui]
sidebar_width = 32
sidebar_min_width = 18
sidebar_max_width = 36
mouse_capture = true
right_click_passthrough_modifier = "ctrl"
mouse_scroll_lines = 3
# Close panes/tabs without prompting.
confirm_close = false
prompt_new_tab_name = true
prompt_new_workspace_name = true
accent = "#c6a0f6"

show_agent_labels_on_pane_borders = false
agent_panel_sort = "spaces"
[ui.toast]
delivery = "herdr"
delay_seconds = 1

[ui.toast.herdr]
position = "top-right"

[ui.toast.clipboard]
enabled = true
position = "bottom-center"

[ui.sound]
# Keep sound notifications disabled.
enabled = false

[session]
# Resume supported coding agents when restoring sessions.
resume_agents_on_restore = true

[remote]
manage_ssh_config = true

[experimental]
# Persist recent pane output to disk.
# This persists recent pane output to disk; keep in mind it may include secrets.
pane_history = true
allow_nested = false
kitty_graphics = true

[advanced]
# Scrollback limit is byte-based.
scrollback_limit_bytes = 52428800
